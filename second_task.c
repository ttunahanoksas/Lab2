#include <stdio.h>
#include ? ? ?

int main()
{
    // declare variables<-- These are comments, you can remove them if you want to

    // get user input

    // calculate the length

    // print the result

    return 0;
}